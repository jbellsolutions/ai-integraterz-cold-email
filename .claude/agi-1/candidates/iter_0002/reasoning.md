# iter_0002 — CI + pre-commit

Targeted: G-Stack Dev Infrastructure (CI gap, +5) + Workflow Coverage (pre-commit, +5)
+ AI-Readiness Verification (CI defined, +5).

Added .github/workflows/ci.yml (Python 3.13 + tests + smoke) and
.pre-commit-config.yaml with .env-block guard and conditional regression-test
hook. No code changes; no destructive edits.
